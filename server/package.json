{
  "dependencies": {
    "express": "^4.18.1"
  },
  "scripts": {
    "start": "node server/index.js",
    "build": "cd client && npm install && npm run build",
    "startDev": "nodemon server/index.js"
  },
  "devDependencies": {
    "nodemon": "^2.0.19"
  }
}
